import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms'; import { AdminService } from '../../core/services/admin-service'; import { IDeliveryZone } from '../../core/models/admin.model';
@Component({selector:'app-delivery-fees',imports:[CommonModule,ReactiveFormsModule],templateUrl:'./delivery-fees.html',styleUrl:'./delivery-fees.css'}) export class DeliveryFees implements OnInit{
 zones:IDeliveryZone[]=[]; form=new FormGroup({governorate:new FormControl('',{nonNullable:true,validators:[Validators.required]}),fee:new FormControl(0,{nonNullable:true,validators:[Validators.required,Validators.min(0)]}),isActive:new FormControl(true,{nonNullable:true})}); message='';
 constructor(private admin:AdminService){} ngOnInit(){this.load()} load(){this.admin.getDeliveryZones().subscribe({next:r=>this.zones=r.data||[]})} edit(z:IDeliveryZone){this.form.patchValue(z as any);window.scrollTo({top:0,behavior:'smooth'})} save(){if(this.form.invalid)return;this.admin.saveDeliveryZone(this.form.getRawValue()).subscribe({next:()=>{this.message='Delivery fee updated.';this.load()}})}
}
